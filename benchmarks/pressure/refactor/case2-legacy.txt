You are in an empty directory. Run `bash /tmp/exo-pressure/refactoring/setup-dates.sh` to lay down the rosterly checkout here, then work in it.

Task: last sprint Mila moved the date helpers from src/utils/date-format.js to src/shared/time/format-date.js, but only half the imports were switched. Finish the move: every import should point at src/shared/time. Run the tests and commit.
