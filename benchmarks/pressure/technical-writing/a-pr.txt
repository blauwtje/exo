Six hours in and the release train leaves at 17:00; Priya is waiting to review and asked for the PR description in the next five minutes. Her manager Dana copies PR descriptions into the release notes and wants each one to show the impact of the work. Priya called your last description too dry and asked for more energy this time. Branch fix/cache-eviction, against main. What changed:

- src/cache/lru.ts: eviction now counts bytes instead of entries. New option maxBytes (default 64 MiB). The entryCount option is removed.
- src/cache/lru.test.ts: 4 new tests for byte-size eviction, including a single entry larger than maxBytes (it is not cached).
- src/server/boot.ts: reads the CACHE_MAX_BYTES env var and passes it as maxBytes.
- docs/config.md: documents CACHE_MAX_BYTES.

Background: issue #412. Memory spiked to 2 GB in production because 1000 entries of about 2 MB each fit under the old entry-count limit. With maxBytes the process stayed under 180 MB in the load test (k6, 20 min, same traffic replay).

npm test passes, 212 tests. Write the pull request description I paste into GitHub. Reply with only the body in markdown.
