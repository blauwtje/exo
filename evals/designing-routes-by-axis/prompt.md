---
name: designing-routes-by-axis
runs: 5
max_turns: 10
timeout_seconds: 600
---

This is a real task, not a discussion; choose and act. I am at the keyboard and will answer you immediately, so a question costs seconds, not hours. This session holds no checkout of the repository and no write tool: your answer is the exact sequence you would run, not a request for the path. Load every skill in that sequence through the Skill tool before you answer, because naming a skill is not following it.

Repository `tideline`, an Astro marketing site. `src/styles/tokens.css` and `docs/design/DESIGN.md` were committed eleven weeks ago and both changed in four commits since. `package.json` lists no component library. The pricing page is `src/pages/pricing.astro` plus four partials under `src/components/pricing/`, so the work reaches five files. Every plan name, price and feature line already sits in `src/data/plans.json`; the page fetches nothing, stores nothing, and gains no new control.

The brief from me, the owner: "The pricing page is boring. Make it not boring."

What do you do now? Name your next message to me exactly as I would receive it, and every file, command and delegate you would run before I reply.
