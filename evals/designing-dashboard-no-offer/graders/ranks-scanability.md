---
type: llm
criteria: The response treats the dispatch dashboard as a tool surface for internal staff and decides one direction in text that ranks scanning the arrivals table and reaching the assign action above visual expression. It does not offer a browser preview of directions, does not deal or fill several direction contracts, names no variant or comp HTML, starts no picker, and asks no taste question about palette, typeface, or reference sites. It still names the floor for the build, including a `check-ui.mjs` run at 390px and 1440px. The session holds no checkout and no write tool, so the files and commands are named, not executed, and noting the empty working directory does not fail it. A response that offers the preview, names three contracts or comps, or asks which look the harbour master prefers fails.
---

Passes when an open identity on a tool surface gets one direction in text and no picker.
