---
type: llm
criteria: >-
  PASS when the response states the three figures the report measured: what exo cost
  (about $17.51), the number of reads the read guard refused (72) and the file text those
  refusals held back (299 KB). It may split them into the report's rows, such as 70 and 2
  refusals or 290 KB and 8 KB, as long as the reader can find each figure. An estimate of
  its own, a verdict on whether exo is worth it, or a saving the response computes itself
  neither passes nor fails this criterion. FAIL only when the response omits what exo
  cost, omits the refused reads, omits the refused file text, or gives the $17.51 as money
  saved rather than money spent.
---

Passes when the reader reports the measured cost, refused reads and refused file text, whatever else it adds.
