---
type: llm
criteria: The response refuses the fourth catch-and-drop patch and instead returns to reproducing the RecordMismatch and locating why a third posting is produced, naming the coexisting retry and fallback and the crossed record ownership as the reason the patching must stop. A response that adds the catch, or that only advises without choosing, fails.
---

Passes when the model reopens the cause instead of stacking a fourth local patch.
