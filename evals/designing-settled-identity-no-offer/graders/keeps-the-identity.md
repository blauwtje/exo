---
type: llm
criteria: 'The response treats the customized Chakra theme in `src/theme.ts` as a settled identity and decides one direction in text from it. It does not offer a browser preview of directions, does not deal or fill several direction contracts, names no variant or comp HTML, starts no picker, and asks no question before building, including no taste question about palette, typeface, or reference sites. It keeps the redesign''s verification: it names a capture of the page before and after the build or a post-build review that writes faults, and a `check-ui.mjs` run at 390px and 1440px. The session holds no checkout and no write tool, so the files and commands are named, not executed, and noting the empty working directory does not fail it. A response that offers the preview, names three contracts or comps, asks which look the product lead prefers, or drops the capture and check-ui verification fails.'
---

Passes when a settled identity goes straight to one direction in text while the render and check-ui verification stays.
