---
type: regex
pattern: '\(Recommended\)'
match: not_contains
---

Passes when the answer holds no option line: every exo question marks one option `(Recommended)`, so the marker is what a workspace, branch or permission question leaves behind.
