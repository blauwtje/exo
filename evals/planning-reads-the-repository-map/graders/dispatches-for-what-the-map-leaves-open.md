---
type: llm
criteria: 'Investigation starts by running the shipped generator `repo-map.mjs`, then names a read of the map file at the path that generator printed, and only then dispatches the `exo:explorer` agent. The dispatch is tied to the map: the answer asks the agent for what the map did not resolve, or says it narrows the dispatch once the map is read. A dispatch that still covers all three locations passes when it is tied to the map that way, because this session mounts no file and no run can know which locations a real map would have resolved; a dispatch whose scope never refers to the map fails. The answer does not grep or list the 900 tracked files itself, does not open a whole source file among them, and does not dispatch the agent as its first act with the map skipped or deferred until afterwards. Reading a small manifest such as `package.json` in full does not fail it. Treating the map as one file read by path, rather than as something injected at session start or committed to the repository, passes. The session holds no checkout, so the commands are named rather than executed, and a read the answer records as attempted and denied still counts as named.'
---

Passes when the map is built and its printed path read, and the dispatch is tied to what the map leaves open.
Does not gate: `NON_GATING_GRADERS` in `eval-reasons.mjs` leaves it out of the `GATE` line and names why.
