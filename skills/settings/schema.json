{
  "specs": {
    "type": "string",
    "options": ["docs", "issues", "both"],
    "default": "docs",
    "description": "where shaping stores a spec: docs/specs files, GitHub issues, or both"
  },
  "replies": {
    "type": "string",
    "options": ["tight", "standard"],
    "default": "tight",
    "description": "how the session writes replies: tight drops preamble, recap and filler; standard keeps full prose"
  },
  "interview": {
    "type": "string",
    "options": ["chat", "page"],
    "default": "chat",
    "description": "where shaping asks its questions: chat asks in the conversation; page opens a browser tab that shows every decision"
  }
}
